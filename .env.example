# Stacksniff configuration variables

# Maximum number of concurrent Playwright Chromium instances
STACKSNIFF_MAX_BROWSERS=3

# Default cache time-to-live (TTL) in seconds for scan results
STACKSNIFF_CACHE_TTL=1800
